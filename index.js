/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './App';

// Register for both native and development client
AppRegistry.registerComponent('SoRita', () => App);
AppRegistry.registerComponent('main', () => App);

